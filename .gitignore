# Cosign private keys — NEVER commit
cosign.key
cosign.private

# Local build artifacts
output/
_build_*
_build-*/**
*.iso*
nelhua-linux-*.iso-CHECKSUM

# Commit message scratch file (read by `git commit -F commit.txt`)
commit.txt

# Local-only design notes / todo list
plan.md

# Editor / OS noise
.DS_Store
*.swp
