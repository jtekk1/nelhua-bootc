# Storage controllers and filesystems available in the initramfs.
add_drivers+=" nvme xhci_pci ahci usb_storage usbhid sd_mod "

# Force-load early — covers the rootfs on btrfs-on-nvme installs.
force_drivers+=" btrfs nvme "
