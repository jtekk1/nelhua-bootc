# Nelhua Linux — first-boot stub config.
# On first login, this triggers the setup wizard. Once the wizard runs,
# the default preset (or user's own config) replaces this file.
exec-once=/usr/libexec/nelhua/check-first-boot.sh
